| $${\color{red}Casing \space Conventions}$$ | | |
|--------|--------|----------|
| | | |
| NAME | VIEW | DESCRIPTION |
| | | |
| camelCase | camelCase | capitalizes the first letter of each word except the first one, and removes spaces or special characters between words |
| snakeCase | snake_case | uses underscores (_) to separate words, making the text lowercase and replacing spaces or special characters |
| capitalCase | CapitalCase | capitalizes the first letter of each word and removes spaces or special characters between words |
| constantCase | CONSTANT_CASE | converts the text to uppercase and uses underscores (_) to separate words |
| dotCase | dot.case | uses dots (.) to separate words, making the text lowercase and replacing spaces or special characters |
| headerCase | Header-Case | capitalizes the first letter of each word and uses hyphens (-) to separate words |
| noCase | no_case | makes the text lowercase and replaces spaces or special characters with underscores (_) |
| paramCase | param-case | uses hyphens (-) to separate words and makes the text lowercase |
| pascalCase | PascalCase | capitalizes the first letter of each word and removes spaces or special characters between words |
| pathCase | /path/case | uses slashes (/) to separate words and makes the text lowercase |
| sentenceCase | Sentence case | capitalizes the first letter of the first word and makes the rest of the text lowercase |
| titleCase | Title Case | capitalizes the first letter of each word, including conjunctions and prepositions |
| lowerCase | lower case | makes all the text lowercase |
| lowerCaseFirst | lowerCASE fIRST| makes the first letter of the text lowercase and keeps the rest of the text unchanged |
| upperCase | UPPERCASE | makes all the text uppercase |
| upperCaseFirst | Uppercase First | makes the first letter of the text uppercase and keeps the rest of the text unchanged |
| spongeCase | SpOnGeCaSe | transforms into a string with random capitalization applied |
| swapCase | sWAPcASE | swaps the case of each letter in the text |
| kebabCase | kebab-case | transforms into a string to lowercase and the hyphen ("-") is used as a separator between words |
| screamKebabCase | SCREAM-KEBAB-CASE | converts to uppercase letters, and the hyphen ("-") is used as a separator between words |
